namespace ProjectCard.Game.Utilities
{
    public enum ThemeType
    {
        Theme1,
        Theme2
    }

    public enum GameState
    {
        None,
        OnStart,
        OnPlay,
        OnQuit
    }
}